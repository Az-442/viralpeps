import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Retatrutide Buying Guides UK | Price Comparison & Supplier Research",
  description:
    "Retatrutide buying guides for UK researchers — where to buy, cheapest price per mg, full supplier price comparison, ordering and TrustScore rankings.",
};

export default function CompoundGuidesLayout({ children }: { children: React.ReactNode }) {
  return children;
}

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Compare Research Peptides UK | Peptide Price Comparison",
  description: "Compare research peptides UK prices side-by-side. Browse every UK research peptide, filter by category, and sort by cheapest supplier price to find the best deals on ViralPeps.",
};

export default function CompoundsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
